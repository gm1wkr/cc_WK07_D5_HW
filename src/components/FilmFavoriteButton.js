import React from "react";

const FilmFavoriteButton = ({onButtonClick}) => {

    const handleButtonClick = (event) => {

    }

    return(
        <button onClick={handleButtonClick}>Add to My Favorites</button>
    )
}

export default FilmFavoriteButton;