import React from "react";

const Footer = () => {
    return(
        <footer>
            <h2>Ghibli Studio API Homework</h2>
            <p>Welcome to  my CodeClan_ Homework.  This small React APP uses data from <a href="https://ghibliapi.herokuapp.com/">Ghibli Studios</a> to show Films in the API and, if available, the cast from a different endpoint.  A Drop of CSS makes things a bit prettier.</p>
        </footer>
    )
}

export default Footer;